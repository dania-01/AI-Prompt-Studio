import { HomePage } from "@/pages-view/HomePage";

export const metadata = {
  title: "Studio — AI Prompt Studio",
  description: "Write prompts, get streaming AI responses, and compare models side-by-side.",
};

export default function StudioPage() {
  return <HomePage />;
}
